[package]
name = "test-nightly_off-std_off"
version = "0.1.0"
edition = "2024"

[dependencies]
colvec = { features = ["allocator-api2"], path = "../../colvec", default-features = false }

[workspace]
